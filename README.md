Follow these instructions to build on your local machine:

- rename .env.example into .env
- add your ALGOLIA_APP_ID, ALGOLIA_API_KEY and ALGOLIA_INDEX to .env
- run those commands:

npm install
npm run build
npm start

Open your browser on http://localhost:3000/ => you'll see the demo app.